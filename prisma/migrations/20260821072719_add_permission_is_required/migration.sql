BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[permission] ADD [is_required] BIT NOT NULL CONSTRAINT [permission_is_required_df] DEFAULT 1;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
