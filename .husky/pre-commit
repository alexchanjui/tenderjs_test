#!/usr/bin/env sh

echo "🔍 執行 lint-staged 處理 Prettier + ESLint..."

npx lint-staged

status=$?

if [ $status -ne 0 ]; then
  echo "❌ lint-staged 錯誤，請修復後再提交。"
  exit $status
fi

echo "✅ 通過 pre-commit 檢查！"